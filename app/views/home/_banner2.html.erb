<div class= "banner-sektion banner-overlay ">
    <div class="container text-center">
        <div class="col-md-12">
            <div class="banner-heading "> 
            <h2 class="pt-md-5 pb-md-4 pt-3 pb-3 pt-md-0 pb-md-5">Trouve le festival qui te correspond le mieux </h2>
            
            <%= form_tag(fests_path, method: :get ) do %>
                <div class= "row justify-content-center">
                <%= text_field_tag :title, params[:title], class: "form-control w-75", placeholder: "Nom d'un festival"  %>
                <%= submit_tag 'Rechercher', class: "btn btn-primary"  do %>
                    <i class="footer-icon fas fa-search"></i>
                <% end %>
                </div>
            <%end%>
            
        <a class="scroll-to btn btn-style-4 btn-primary btn-lg mt-5" href="#services">En savoir plus <i class="fas fa-arrow-down"></i></a>
      </div>
          </div>
          </div>
        </div>
      </div>

      