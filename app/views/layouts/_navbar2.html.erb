
<div class="navbar-wrapper navbar-boxed navbar-sticky">
      <div class="container-fluid">
        <div class="d-table-cell align-middle pr-md-3">
          <a class="navbar-brand mr-1" href="#"><img src="<%= root_path%>" alt="FestRoad"></a>
        </div>
        <div class="d-table-cell w-100 align-middle pl-md-3">
          <div class="navbar justify-content-end justify-content-lg-between">
            <ul class="navbar-nav d-none d-lg-block">
              <li class="nav-item mega-dropdown-toggle"><a class="nav-link" href="<%= root_path%>">Accueil</a>
              </li>
              <li class="nav-item"><a class="nav-link" href="#">Les festivals</a>
              </li>
              <li class="nav-item"><a class="nav-link" href="#">Contact</a>
              </li>
            </ul>

           <% if current_user%>
          <!-- Components-->
            <%= link_to "Se déconnecter",  destroy_user_session_path, method: :delete, class:"btn btn-gradient ml-3 d-none d-xl-inline-block" %></li>
          <%else%>
            <a class="btn ml-3 d-none d-xl-inline-block" href="<%= new_user_session_path %>">Me connecter</a>
          <%end%>
          </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

